# Add this to your existing .env file

# Face Embedding Encryption Key (32 bytes for AES-256)
# Generate a new key with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
EMBEDDING_ENCRYPTION_KEY=your_32_byte_hex_key_here
